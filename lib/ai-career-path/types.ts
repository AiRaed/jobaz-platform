/**
 * @deprecated Import from `@/lib/jobaz-ai` instead.
 * Re-exports for backward compatibility during migration.
 */
export type {
  AssessmentAnswers as CareerPathAnswers,
  CareerAssessmentResult as CareerPathResult,
  CareerAssessmentScores as CareerPathScores,
  CareerPathType as PathProfileId,
  RecommendedTool,
  SituationAnswer,
  ExperienceAnswer,
  EnglishAnswer,
  CvAnswer,
  HelpNextAnswer,
} from '@/lib/jobaz-ai'
